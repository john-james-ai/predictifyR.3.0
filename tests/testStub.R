# getData(force = FALSE)
# cleanCorpus(corpora$raw, corpora$clean)
# analysis <- analyzeData(corpora$clean)
# fileName <- 'Clean Corpus Analysis'
# objName <- 'cleanCorpusAnalysis'
# logResults(analysis, fileName, objName)
